name: copy
description: >-
  Simplifies the copyWith pattern: ValueGetter-based updates that can
  set nullable fields to null. Interoperable with Flutter's ValueGetter.
version: 1.1.0
repository: https://github.com/tembeon/tem_tools

environment:
  sdk: '>=3.10.1 <4.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

name: json
description: >-
  Type-safe JSON parsing over an extension type: inferred typed access,
  fallbacks, nested object and list parsing, and path traversal.
version: 1.1.0
repository: https://github.com/tembeon/tem_tools

environment:
  sdk: '>=3.10.1 <4.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
